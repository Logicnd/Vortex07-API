{
  "name": "vortex07-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create vortex07",
    "db:migrate:local": "wrangler d1 execute vortex07 --local --file=./schema.sql",
    "db:migrate": "wrangler d1 execute vortex07 --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
