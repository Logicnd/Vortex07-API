import { createHmac, timingSafeEqual } from "node:crypto";function secret() {return ( process.env.ECONOMY_SECRET || process.env.VORTEX07_VOTE_SECRET || "vortex07-vote-hmac-v2-5-0-playvortex" );} export function verifyBotHmac(request, { action, discordId }) {const ts = request.headers.get("x-vortex07-timestamp") || "";const sig = request.headers.get("x-vortex07-signature") || "";if (!ts || !sig || !discordId) return false;const age = Math.abs(Date.now() - Number(ts));if (!Number.isFinite(age) || age > 10 * 60_000) return false;const payload = `economy|${action}|${discordId}|${ts}`;const expected = createHmac("sha256", secret()).update(payload).digest("hex");try {const a = Buffer.from(sig, "hex");const b = Buffer.from(expected, "hex");if (a.length !== b.length) return false;return timingSafeEqual(a, b);} catch {return false;}} export function isSnowflake(id) {return /^\d{17,20}$/.test(String(id || ""));} export function parsePlayvortexId(value) {const n = Number(value);if (!Number.isInteger(n) || n < 1) return null;return n;}