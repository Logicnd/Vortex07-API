import { getThread, getUnreadCount, listInbox, sendMessage } from "../../../lib/dm.js"; const CORS={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Methods":"GET, POST, OPTIONS","Access-Control-Allow-Headers":"Content-Type"}; function json(data,status=200){return Response.json(data,{status,headers:CORS});} async function readBody(request){try{return await request.json();}catch{return {};}} async function resolveOp(request,context){ const url=new URL(request.url);let op=url.searchParams.get("op"); if(!op){const m=url.pathname.match(/\/(?:api\/)?v1\/dm\/([^/?#]+)/i);if(m)op=decodeURIComponent(m[1]);} if(!op){try{const params=await context?.params;if(params?.op)op=String(params.op);}catch{}} return{op:op||null,peerId:url.searchParams.get("peerId"),actorId:url.searchParams.get("actorId")}; } export function OPTIONS(){return new Response(null,{status:204,headers:CORS});} export async function GET(request,context){ const{op,peerId,actorId}=await resolveOp(request,context); try{ if(op==="inbox"){const r=await listInbox(actorId);return json(r,r.ok?200:(r.status||400));} if(op==="unread"){const r=await getUnreadCount(actorId);return json(r,r.ok?200:(r.status||400));} if(op==="thread"){const r=await getThread(actorId,peerId);return json(r,r.ok?200:(r.status||400));} return json({ok:false,error:"not-found",op},404); }catch(err){console.error("dm GET failed",err);return json({ok:false,error:"db-error",message:String(err?.message||err)},500);} } export async function POST(request,context){ const{op,peerId}=await resolveOp(request,context);const body=await readBody(request); try{ if(op==="send"){const r=await sendMessage({actorId:body?.actorId,peerId:peerId??body?.peerId,authorName:body?.authorName,peerName:body?.peerName,body:body?.body});return json(r,r.ok?201:(r.status||400));} return json({ok:false,error:"not-found",op},404); }catch(err){console.error("dm POST failed",err);return json({ok:false,error:"db-error",message:String(err?.message||err)},500);} }